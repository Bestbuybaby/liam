interface:
  display_name: "Liam"
  short_description: "Repo-specific patterns and workflows for liam"
  default_prompt: "Use the liam repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true